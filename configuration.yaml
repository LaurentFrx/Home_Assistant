# === Composants par défaut essentiels ===
default_config:

# === Inclusion des notification mobiles ===
mobile_app:

# === Personnalisation de Home Assistant ===
homeassistant:
  packages: !include_dir_named packages    
  customize:
    climate.thermostat_sdb:
      friendly_name: ""
    sensor.energie_entrant_dans_la_solarbank:
      device_class: energy
      state_class: total_increasing
      unit_of_measurement: kWh
    sensor.energie_sortant_de_la_solarbank:
      device_class: energy
      state_class: total_increasing
      unit_of_measurement: kWh

# === Thèmes personnalisés ===
frontend:
  themes: !include_dir_merge_named themes

# === Scripts, automatisations et templates ===
script: !include_dir_merge_named scripts
automation: !include_dir_merge_list automations
template: !include_dir_merge_list templates

# === Dashboards Lovelace ===
lovelace:
  dashboards:
    # Dashboard Cumulus unifié - Toutes les variantes en un seul dashboard
    lovelace-cumulus:
      mode: yaml
      title: Cumulus Intelligent
      icon: mdi:water-boiler
      show_in_sidebar: true
      filename: lovelace/dashboard_cumulus_complet.yaml
    
    # Dashboard Cumulus Corrigé - Version optimisée avec templates avancés
    lovelace-cumulus-corrige:
      mode: yaml
      title: Cumulus Corrigé (Avancé)
      icon: mdi:water-boiler-alert
      show_in_sidebar: true
      filename: lovelace/cards/dashboard_cumulus_corrige.yaml

# === Journalisation ===
logger:
  default: warning
